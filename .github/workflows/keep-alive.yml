name: Keep Alive - Full Pipeline

on:
  schedule:
    - cron: '*/14 * * * *'

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Render Backend
        run: |
          curl -f https://your-render-app.onrender.com/health || exit 1

      - name: Ping Vercel Module
        run: |
          curl -f https://tool-hyev3v3cz-bacard1is-projects.vercel.app || exit 1